import 'package:flutter/material.dart';

class HeaderBase extends StatelessWidget {
  const HeaderBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  const Text('header');
  }
}